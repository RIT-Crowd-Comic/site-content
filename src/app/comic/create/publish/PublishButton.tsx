

"use client"
const PublishButton = ({ }) => {
    const onClick = () => {
        
    }

    return (
        <button onClick={onClick} id="publish-btn">Publish</button>
    );
}

export default PublishButton