let panelSetPosition = 0; //reading first panel set of trunk

//by default (first landing of page), panels display with default values
//refreshes images by changing image src values to current comic panel pathways
const UpdatePanelSet = (firstImgPath = "/comic-panels/first_panel.png", secondImgPath, thirdImgPath) =>{

    return;
}

//function to populate branch hooks on new panels in specified panel + place

export default UpdatePanelSet;